const PUBLIC_ROUTES = ["/", "/barbearias/[id]"];
const AUTH_ROUTES = ["/signin", "/signup"];
const API_AUTH_PREFIX = "/api/auth";
const DEFAULT_LOGIN_REDIRECT = "/";

export { PUBLIC_ROUTES, DEFAULT_LOGIN_REDIRECT, AUTH_ROUTES, API_AUTH_PREFIX };
